# visual-contract

## What it is
TODO

## Run locally
```bash
cd ~/dev/saas/visual-contract
# install dependencies
# start dev server
```

Shared services: `devdb up` (Postgres / MySQL / Redis / Mailpit on localhost)

## Environment
Copy `.env.example` to `.env.local` and fill in the values.

## Deploy
- Platform: TODO
- Domain: TODO
- Environment variables: TODO
