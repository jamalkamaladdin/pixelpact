---
'pixelpact-core': patch
'pixelpact': patch
'pixelpact-mcp': patch
---

Point the package descriptions and keywords at the case the tool is built for: a coding agent
that writes UI code and cannot see the result. The behaviour is unchanged.
